[package]
name = "thrsh-core"
version = "0.1.0"
edition = "2021"
description = "Cross-market prediction market arbitrage engine"
license = "MIT"

[lib]
crate-type = ["cdylib", "lib"]
name = "thrsh_core"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
